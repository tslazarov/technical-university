<Page
    x:Class="SmarterThanYou.Mobile.Views.RegisterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmarterThanYou.Mobile.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="../Assets/background.png"></ImageBrush>
        </Grid.Background>
        <StackPanel VerticalAlignment="Center" Width="300">
            <TextBox Background="Transparent" PlaceholderText="Username" Text="{Binding Path=CurrentUser.Username, Mode=TwoWay}"/>
            <PasswordBox Background="Transparent" PlaceholderText="Password" Password="{Binding Path=CurrentUser.Password, Mode=TwoWay}" Margin="0, 10, 0, 10"/>
            <Button Name="btnRegister" Background="White" Foreground="Black" Content="Register" HorizontalAlignment="Stretch" Click="btnRegister_Click"/>
        </StackPanel>
    </Grid>
</Page>
